import { Goal, Task, TimeBlock, FocusSession } from './types';

export const MOCK_GOALS: Goal[] = [];

export const MOCK_TASKS: Task[] = [];

export const MOCK_TIME_BLOCKS: TimeBlock[] = [];

export const MOCK_SESSIONS: FocusSession[] = [];
